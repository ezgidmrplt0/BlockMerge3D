// R8/D8 ÜST GEÇİŞİ — Firebase (kotlin-stdlib 2.0.x) D8 StackOverflow düzeltmesi.
// AGP 7.4.2'nin içindeki eski D8, kotlin-stdlib 2.0.21'i dex'lerken StackOverflowError veriyor
// (bkz. AdMob ile aynı duvar). Daha yeni bir R8'i buradan classpath'e zorlayarak dexing motorunu
// güncelliyoruz — Unity/AGP sürümünü yükseltmeden. R8 sürümü AGP 7.4.2 ile uyumlu olmalı; hata
// çıkarsa sürümü düşür/yükselt (8.2.x–8.5.x aralığı denenebilir).
buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools:r8:8.3.37'
    }
}

plugins {
    // If you are changing the Android Gradle Plugin version, make sure it is compatible with the Gradle version preinstalled with Unity
    // See which Gradle version is preinstalled with Unity here https://docs.unity3d.com/Manual/android-gradle-overview.html
    // See official Gradle and Android Gradle Plugin compatibility table here https://developer.android.com/studio/releases/gradle-plugin#updating-gradle
    // To specify a custom Gradle version in Unity, go do "Preferences > External Tools", uncheck "Gradle Installed with Unity (recommended)" and specify a path to a custom Gradle version
    id 'com.android.application' version '7.4.2' apply false
    id 'com.android.library' version '7.4.2' apply false
    **BUILD_SCRIPT_DEPS**
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
